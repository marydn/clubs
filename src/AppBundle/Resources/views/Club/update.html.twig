{% extends "::base.html.twig" %}

{% block title %}Modificando Club: {{ club.name }}{% endblock %}

{% block body %}
    <h1>Modificando Club: {{ club.name }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit">Actualizar</button>
    {{ form_end(form) }}
{% endblock %}
